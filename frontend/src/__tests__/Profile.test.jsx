import { render, screen, fireEvent } from "@testing-library/react";
import Profile from "../pages/Profile";

describe("Profile Page", () => {
  it("renders user details", () => {});
  it("updates risk appetite", () => {});
  it("renders AI recommendations", () => {});
});
