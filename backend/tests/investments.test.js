const request = require("supertest");
const app = require("../app");

describe("Investments Module", () => {
  it("invests in a product successfully", async () => {});
  it("fetches user portfolio", async () => {});
  it("generates portfolio insights", async () => {});
});
